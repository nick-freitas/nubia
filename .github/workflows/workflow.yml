name: Nx Affected CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [12.x]
    steps:
      - uses: mansagroup/nrwl-nx-action@v2
        with:
          targets: lint,build,deploy
