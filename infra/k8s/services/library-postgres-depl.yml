apiVersion: apps/v1
kind: Deployment
metadata:
  name: library-postgres-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: library-postgres
  template:
    metadata:
      labels:
        app: library-postgres
    spec:
      containers:
        - name: library-postgres
          image: postgres
          env:
            - name: POSTGRES_DB
              value: postgresdb
            - name: POSTGRES_USER
              value: postgresadmin
            - name: POSTGRES_PASSWORD
              value: admin123
            - name: POSTGRES_HOST_AUTH_METHOD
              value: trust
          resources:
              limits:
                cpu: "500m"
                memory: "2G"
---
apiVersion: v1
kind: Service
metadata:
  name: library-postgres-srv
spec:
  selector:
    app: library-postgres
  ports:
    - name: library-db
      protocol: TCP
      port: 5432
      targetPort: 5432